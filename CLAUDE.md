# Bank It

See `README.md` for the Python analysis scripts (`bank_it.py`,
`plot_strategy_scores.py`, `plot_oracle_scores.py`), what each mode does, and
how to run the web app locally. See `LEARNINGS.md` for the full narrative
write-up behind the strategy research.
